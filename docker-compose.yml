services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - legacy-data:/app/data
      - legacy-media:/app/media
      - legacy-backups:/app/backups
    restart: unless-stopped

volumes:
  legacy-data:
  legacy-media:
  legacy-backups:
